
import styled from 'styled-components'

import {Label} from "../../utils/styled";

export const LabelTitle = styled(Label).attrs({
    className:'label-title',
    bold:true,
    color:'black-6'
})`

`
